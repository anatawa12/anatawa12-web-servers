version: '3'
services:
  web-frontend:
    build: 'frontend'
    image: 'web-frontend'
    ports:
       - '80:80'
  web-nico-ranking:
    image: 'ghcr.io/anatawa12/zip-http-server'
    volumes: 
      - './resources/nico-ranking.zip:/root.zip'
